Question 1: What is the keyword for extracting the day of the month?
Answer: getDate

Question 2: What is the keyword for extracting the year?
Answer: getFullYear

Question 3: What is the keyword for extracting the milliseconds since midnight, Jan. 1, 1970?
Answer: getTime

Question 4: What is the keyword for extracting the smallest unit of time that JavaScript recognizes?
Answer: getMilliseconds

Question 6: Code a statement that extracts the hours from the Date object represented by dObj and assigns it to hrs, which hasn't been declared beforehand.
Answer: var hrs = Dobj.getHours();

Question 7: Code a statement that extracts whether it's Monday, Tuesday, etc. (as a number) from the Date object represented by d and assigns it to x, which hasn't been declared beforehand.
Answer: var x = d.getDay();

Question 8: The Date object is represented by d. Code a statement that displays the year in an alert.
Answer: alert(d.getFullYear());

Question 9: The Date object is represented by now. Code the first line of an if statement that tests whether it's the last quarter of the year.
Answer: if (now.getMonth() > 8) {

Question 10: Code a statement that determines the seconds since midnight, Jan. 1, 1970 and assigns it to a variable that hasn't been declared beforehand. Use any variable you like to represent the Date object. Note that I'm asking for seconds. Don't round it.
Answer: var secsSince = d.getTime() / 1000;

Question 11: Code the first line of an if statement that tests whether it's before noon. Use any variable you like to represent the Date object.
Asnwer: if now.getHours() < 12) {

Question 12: Code the first line of an if statement that tests whether the clock is at an hour on the dot. Check minutes and then seconds. Ignore milliseconds. Use any variable you like to represent the Date object.
Answer: if( d.getMinutes() === 0 && d.getSeconds() === 0) {

Question 19:Create a new Date object and assign it to a variable.
Code an alert that displays the current year.
Click the Run It button to run your code live.
Answer: var newDate = new Date();
alert(newDate.getFullYear());

Question 20: Create a new Date object.
Extract the month.
Convert the month to a month name.
Code an alert that displays the month name.
Click the Run It button to run your code live.
Answer: var moNames = ["Jan", "Feb", "Mar", "Apr","May",
    "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
var d = new Date();
var theMo = d.getMonth();
alert(moNames[theMo]);
